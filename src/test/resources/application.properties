spring.metrics.doppler.host: ${DOPPLER_HOST:${CF_INSTANCE_IP:localhost}}
spring.metrics.doppler.port: ${DOPPLER_PORT:3457}
spring.metrics.doppler.secret:
# loggregator-secret
spring.metrics.doppler.origin: metrics-sample
